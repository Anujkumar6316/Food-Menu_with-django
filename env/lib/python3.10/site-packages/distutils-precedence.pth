import os
import dotenv

# Load environment variables from .env file
dotenv.load_dotenv()

var_name = "SETUPTOOLS_USE_DISTUTILS"
expected_local_value = "local"

# Get the environment variable value
env_value = os.getenv(var_name, "stdlib")

# Check if the environment variable is set to 'local'
if env_value == expected_local_value:
    from _distutils_hack import add_shim 
    add_shim()
